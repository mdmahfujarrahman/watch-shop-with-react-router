import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h4>I am your Uncle</h4>
            <p>House: {house}</p>
        </div>
    );
};

export default Uncle;